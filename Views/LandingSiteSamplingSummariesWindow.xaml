<Window x:Class="NSAP_ODK.Views.LandingSiteSamplingSummariesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NSAP_ODK.Views"
        mc:Ignorable="d"
        Title="LandingSiteSamplingSummariesWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        <Label x:Name="labelTitle" HorizontalAlignment="Center" FontWeight="Bold" Content="This is the title" VerticalAlignment="Center"/>
        <DataGrid x:Name="dataGrid" Grid.Row="1" LoadingRow="OnGridLoadingRow" ItemsSource="{Binding}"/>
        <WrapPanel Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10">
            <Button Content="Cancel"  Width="50" Height="25" Click="OnButtonClick" x:Name="buttonCancel"/>
            <Button Content="Delete selected" Width="90" Height="25" Click="OnButtonClick" x:Name="buttonDelete" Margin="10 0"/>
            <Button Content="Ok" Width="50" Height="25" Click="OnButtonClick" x:Name="buttonOk"/>
        </WrapPanel>
    </Grid>
</Window>
