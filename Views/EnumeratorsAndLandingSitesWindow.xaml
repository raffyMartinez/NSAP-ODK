<Window x:Class="NSAP_ODK.Views.EnumeratorsAndLandingSitesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NSAP_ODK.Views"
        mc:Ignorable="d"
        Title="Enumerators and landing sites" Height="450" Width="560">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Label x:Name="labelTitle" Content="Enumerator name here" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
        <DataGrid x:Name="dataGrid" Grid.Row="1" ItemsSource="{Binding}"/>
        <Button x:Name="buttonOk" Content="Ok" Height="25" Width="40" Grid.Row="2" HorizontalAlignment="Right" Margin="0 0 10 0" Click="onButtonClick"/>
    </Grid>
</Window>
