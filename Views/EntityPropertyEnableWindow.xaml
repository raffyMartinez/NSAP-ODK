<Window x:Class="NSAP_ODK.Views.EntityPropertyEnableWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NSAP_ODK.Views"
        mc:Ignorable="d"
        Closing="ClosingTrigger"
        Title="EntityPropertyEnable" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="6*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ScrollViewer Margin="0,10,0,30" VerticalScrollBarVisibility="Visible" Grid.Row="1">
            <StackPanel  x:Name="PanelChecks" Margin="20,5,10,5"/>
        </ScrollViewer>
        <StackPanel Grid.Column="1" Grid.Row="1">
            <Button Content="OK" Margin="10" Height="30" Click="OnButtonClick" x:Name="ButtonOK"/>
            <Button Content="Cancel" Margin="10" Height="30" Click="OnButtonClick" x:Name="ButtonCancel"/>
        </StackPanel>
        <Label Grid.ColumnSpan="2" Content="This is a label" x:Name="LabelTitle" VerticalAlignment="Center" Margin="10,0,0,0" />
    </Grid>
</Window>
